#!/usr/bin/env python3

def to_str(n:float) ->str:
  """It takes a number and convert it to string
  Args:
      n (float): number to be string
  Returns:
      str: it return a converted string
  """
  return str(n)
